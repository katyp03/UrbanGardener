<% if current_user == @post.user %>
  <h1>Editing Post</h1>

  <%= render 'form', post: @post %>

  <%= link_to 'Show', @post %> |
  <%= link_to 'Back', posts_path %>
<% else %>
  <center>
    This isn't your post to edit!!!
  </center>
<% end %>
